## 🧩 一、你当前仓库结构（cpdf 分支分析）

```
auto-doc-demo_mac_win/
├── tools/
│   ├── build_pdf.py         ← 主构建脚本（调用 latex_inject、render_rst）
│   ├── render_rst.py        ← CSV → RST 生成逻辑
│   ├── latex_inject.py      ← 自动注入 LaTeX 样式（v3.6 企业版）
│
├── csv-input/
│   └── at_N706B.csv         ← N706B 命令定义文件
│
├── docs/
│   ├── _common/
│   │   ├── conf_common.py   ← 字体、Logo、版权等统一参数
│   │   └── _static/         ← 背景、Logo、页眉图像
│   │
│   ├── N706B/
│   │   ├── source/
│   │   │   ├── conf.py      ← 当前型号配置，支持自动注入
│   │   │   └── *.rst        ← CSV 转换输出的命令文档
│   │   └── build/
│   │       ├── latex/
│   │       └── pdf/
│   │
│   └── N725/                ← 可复用结构（未来扩展）
│
└── README_BuildDocs.md      ← 构建说明文档
```

### ✅ 优点

* 工具链简洁明了，维护方便。
* 结构统一，适合复制新型号目录快速复用。
* `_common` 模块抽象得当，支持字体/样式集中管理。
* PDF 输出已自动化，无需手动干预。

---

## ⚙️ 二、潜在问题（针对未来多型号、多语言场景）

| 模块                      | 问题点      | 说明                                     |
| ----------------------- | -------- | -------------------------------------- |
| `tools/build_pdf.py`    | 单一型号写死   | 目前 `MODEL_NAME="N706B"` 固定，扩展性受限。      |
| `tools/latex_inject.py` | 中文封面固定模板 | 不支持英文封面、不同品牌样式。                        |
| `docs/_common`          | 缺少语言区分   | 若未来要 en / zh_CN，需要多语言模板与资源。            |
| 输出结构                    | 仅支持 PDF  | 缺少 HTML 统一构建；不能 CI/CD 部署。              |
| CI                      | 无持续集成    | 没有 GitHub Actions 或 GitLab CI 自动构建与上传。 |
| 模型切换                    | 手工操作     | 新型号要改多个地方（CSV、路径、conf.py）。             |

---

## 🧱 三、推荐企业级统一架构（多型号 + 多语言 + CI 版本化）

```
auto-doc-enterprise/
├── tools/
│   ├── build_docs.py           # 🌍 新主脚本：支持多型号/多语言自动构建（PDF+HTML）
│   ├── render_rst.py           # CSV → RST 生成器（通用）
│   ├── latex_inject.py         # LaTeX 样式注入器（v4.0 国际化增强版）
│   ├── config_loader.py        # 📦 统一读取模型配置（可扩展 JSON/YAML）
│   └── utils/
│       └── logging_helper.py   # 日志格式、彩色输出
│
├── csv-input/
│   ├── N706B/at_N706B.csv
│   ├── N725/at_N725.csv
│   └── N708C/at_N708C.csv
│
├── docs/
│   ├── _common/
│   │   ├── conf_common.py      # 字体、公司信息、通用参数
│   │   ├── templates/          # 统一模板文件夹（可分语言）
│   │   │   ├── zh_CN/
│   │   │   │   ├── base_page.j2
│   │   │   │   └── command_page.j2
│   │   │   └── en/
│   │   │       ├── base_page.j2
│   │   │       └── command_page.j2
│   │   └── _static/            # 所有公共图像、LOGO、样式文件
│   │
│   ├── N706B/
│   │   ├── zh_CN/
│   │   │   └── source/
│   │   │       ├── conf.py
│   │   │       ├── *.rst
│   │   │       └── index.rst
│   │   └── en/
│   │       └── source/
│   │           ├── conf.py
│   │           ├── *.rst
│   │           └── index.rst
│   │
│   ├── N725/...
│   └── versions.json           # 📜 管理不同版本（v1.4, v1.5...）
│
├── .github/
│   └── workflows/
│       └── build-docs.yml      # 🚀 GitHub Actions 自动构建 PDF/HTML + 上传
│
└── README_BuildDocs.md
```

---

## 🚀 四、工作流程（企业级自动构建）

### 🧠 核心思想

所有文档构建都由 `tools/build_docs.py` 统一控制：

```bash
python tools/build_docs.py --model N706B --lang zh_CN --format pdf
```

### 流程：

1️⃣ 读取 `config_loader.py` 配置（根据型号 → 找对应 CSV + 模板 + conf）
2️⃣ 自动执行：

* CSV → RST
* 注入 LaTeX 样式（调用 `latex_inject.py`）
* 运行 `sphinx-build`
* 生成 PDF 或 HTML
  3️⃣ 将结果放入：

```
docs/<MODEL>/<LANG>/build/pdf/
docs/<MODEL>/<LANG>/build/html/
```

4️⃣ CI 自动上传：

* PDF → GitHub Release
* HTML → Pages 或内网 Docs Portal

---

## 🧰 五、国际化与版本化策略

### 🌐 多语言模板建议

在 `_common/templates/` 下区分 `zh_CN` 与 `en`，可通过 Jinja2 实现：

```jinja2
{% if lang == "zh_CN" %}
命令格式
^^^^^^^^
{% else %}
Command Syntax
^^^^^^^^^^^^^^
{% endif %}
```

### 🗂️ 版本管理方案

添加 `versions.json`：

```json
{
  "N706B": ["v1.3", "v1.4", "v1.5"],
  "N725": ["v1.0", "v1.1"]
}
```

脚本自动选择最新版本生成。

---

## 🤖 六、CI/CD 集成建议（GitHub Actions）

`.github/workflows/build-docs.yml` 示例：

```yaml
name: Build Documentation
on:
  push:
    branches: [main, release/*]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          sudo apt-get install texlive-xetex
      - name: Build PDF (CN)
        run: |
          python tools/build_docs.py --model N706B --lang zh_CN --format pdf
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: N706B_PDF
          path: docs/N706B/zh_CN/build/pdf/
```

---

## 🧭 七、最终成效

| 项目目标  | 实现方式                    | 结果          |
| ----- | ----------------------- | ----------- |
| 多型号   | 统一目录结构 + 参数化脚本          | 自动识别型号构建    |
| 多语言   | 模板国际化 + 路径分语言           | 同源生成中英两版    |
| 跨平台   | macOS / Windows / Linux | 字体自适应       |
| 自动化   | GitHub Actions          | 构建、打包、上传全自动 |
| 版本管理  | versions.json + CI tag  | 一键生成多版本 PDF |
| 企业品牌化 | 公共模板 `_common`          | 样式统一，版权一致   |

